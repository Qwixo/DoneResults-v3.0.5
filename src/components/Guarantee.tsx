import React from 'react';

const Guarantee: React.FC = () => {
  return (
    <section id="guarantee" className="py-16">
      <div className="container mx-auto">
        <h2 className="text-3xl font-bold text-center mb-8">Our Guarantee</h2>
        <p>If we don't generate at least 10 qualified meetings for your business in the first 60 days, we'll continue working for free until we do.</p>
      </div>
    </section>
  );
};

export default Guarantee;
